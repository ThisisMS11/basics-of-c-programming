#include <stdio.h>


#define mohit 19 
//this is going to be a preprocessor command so now the value of mohit cannot be redefined ever again.
int main(int argc, char const *argv[])
{
    // const int a=4; 
    // a=45;               this statement is invalid because we have used the const with a.
    // printf("the value of a is %d",a);


    // int mohit=0; this statement is invalid

    printf(" \a");

    return 0;
}
